name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore dependencies
        run: dotnet restore UrlShortener.sln

      - name: Build
        run: dotnet build UrlShortener.sln --no-restore -c Release

      - name: Run Tests
        run: dotnet test UrlShortener.sln --no-build -c Release --verbosity normal

      - name: Build Docker image
        run: |
          docker build \
            -f Shorten.Redirect/Dockerfile.txt \
            -t loganlee23011/url-shortener:${{ github.sha }} \
            -t loganlee23011/url-shortener:latest \
            .

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: loganlee23011
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Push Docker image
        run: |
          docker push loganlee23011/url-shortener:${{ github.sha }}
          docker push loganlee23011/url-shortener:latest

      - name: Deploy to Render
        run: |
          curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK }}"
    
